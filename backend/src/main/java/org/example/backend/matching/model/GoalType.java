package org.example.backend.matching.model;

public enum GoalType {
    // 보조 목표
    NEW_CUSTOMER,
    VIP_BENEFIT,
    ROOM_BOOKING,
    APP_JOIN,
    MEMBER_REVISIT,
    BRAND_EXPOSURE,
    REVENUE
}
